import { Box, Button } from '@material-ui/core';
import React from 'react';

const Color = () => {
    return (
        <>
            <Box variant='circle'>
                <Button />
            </Box>
        </>
    );
};

export default Color;